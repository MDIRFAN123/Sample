name: workbook-validation-cops
description: COPS Workbook Validation application

commands:
  default:
    unix: python main.py
    supports_http_options: true

dependencies:
  - python=3.10
  - pip
  - pip:
      - -r requirements.txt

platforms:
  - linux-64
